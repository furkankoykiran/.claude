<!-- AUTO-GENERATED by `bun run catalog:generate` — do not edit directly. -->
<!-- Regenerate: bun run catalog:generate -->
<!-- Source: gstack · revision a3259400a366593e0c909dd9ac3e59752efd2488 -->
<!-- Redistribution: full (declared MIT, detected MIT) -->

# /unfreeze

| field | value |
| --- | --- |
| skill-name | unfreeze |
| source | gstack |
| pack | gstack |
| repo | https://github.com/garrytan/gstack.git |
| ref | origin/HEAD |
| resolved-revision | a3259400a366593e0c909dd9ac3e59752efd2488 |
| relative-path | unfreeze/SKILL.md |
| version | 0.1.0 |
| allowed-tools | Bash, Read |
| digest | sha256:3ea6546088b0bf36fa0e5e131f7f03017bfdfa4afddcc9518fcba330c61df9e6 |
| redistribution | full |
| security-flags | bash/powershell tool |

## Description

Clear the freeze boundary set by /freeze, allowing edits to all directories again. (gstack)

## Original frontmatter

```yaml
name: unfreeze
version: 0.1.0
description: Clear the freeze boundary set by /freeze, allowing edits to all directories again. (gstack)
triggers:
  - unfreeze edits
  - unlock all directories
  - remove edit restrictions
allowed-tools:
  - Bash
  - Read
```

## Instructions

<!-- AUTO-GENERATED from SKILL.md.tmpl — do not edit directly -->
<!-- Regenerate: bun run gen:skill-docs -->


## When to invoke this skill

Use when you want to widen edit scope without ending the session.
Use when asked to "unfreeze", "unlock edits", "remove freeze", or
"allow all edits".

# /unfreeze — Clear Freeze Boundary

Remove the edit restriction set by `/freeze`, allowing edits to all directories.

```bash
mkdir -p ~/.gstack/analytics
echo '{"skill":"unfreeze","ts":"'$(date -u +%Y-%m-%dT%H:%M:%SZ)'","repo":"'$(basename "$(git rev-parse --show-toplevel 2>/dev/null)" 2>/dev/null || echo "unknown")'"}'  >> ~/.gstack/analytics/skill-usage.jsonl 2>/dev/null || true
```

## Clear the boundary

```bash
eval "$(~/.claude/skills/gstack/bin/gstack-paths)"
STATE_DIR="$GSTACK_STATE_ROOT"
if [ -f "$STATE_DIR/freeze-dir.txt" ]; then
  PREV=$(cat "$STATE_DIR/freeze-dir.txt")
  rm -f "$STATE_DIR/freeze-dir.txt"
  echo "Freeze boundary cleared (was: $PREV). Edits are now allowed everywhere."
else
  echo "No freeze boundary was set."
fi
```

Tell the user the result. Note that `/freeze` hooks are still registered for the
session — they will just allow everything since no state file exists. To re-freeze,
run `/freeze` again.

---

*Generated by the Skills Catalog resolver. Edit the upstream source, then rerun `bun run catalog:generate`.*
